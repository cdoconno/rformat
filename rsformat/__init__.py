"""
rsformat is a helper to format and manage datasets.
The goal is to allow configuration based filtering to provide
a layer of abstraction from queried result sets if those sets
need small changes
"""
import os
from . import resultset

name = "rsformat"
__version__ = '0.0.1'
